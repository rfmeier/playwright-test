import { test, expect } from '@playwright/test';

test('login to WordPress', async ({ page }) => {
	// // Go to http://localhost:8888/wp-login.php
	// await page.goto('http://localhost:8888/wp-login.php');

	// // Click input[name="log"]
	// await page.locator('input[name="log"]').click();

	// // Fill input[name="log"]
	// await page.locator('input[name="log"]').fill('admin');

	// // Click input[name="pwd"]
	// await page.locator('input[name="pwd"]').click();

	// // Fill input[name="pwd"]
	// await page.locator('input[name="pwd"]').fill('password');

	// // Check input[name="rememberme"]
	// await page.locator('input[name="rememberme"]').check();

	// // Click text=Log In
	// await page.locator('text=Log In').click();
	// await expect(page).toHaveURL('http://localhost:8888/wp-admin/');

	// // Click h1:has-text("Dashboard")
	// await page.locator('h1:has-text("Dashboard")').click();
});
